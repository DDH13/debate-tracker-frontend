# Base URL for the debate-tracker backend API.
# Include the API version path. No trailing slash.
# When unset, the app falls back to http://localhost:8080/api/v1
REACT_APP_API_BASE_URL=http://localhost:8080/api/v1
